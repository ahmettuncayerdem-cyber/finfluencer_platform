# V1.0 Staging Plan

Classifies every modified and untracked path from `git status` into one of eight categories. No staging or commits have been performed. `git add` is not run in this document — commands are listed for later use, once you approve each commit individually.

Baseline: full `git status --porcelain` output captured this session, cross-referenced against `V1.0_Consolidated_Remediation_Plan.md` §5 and the self-review findings from earlier in this sprint.

---

## 1. Commit for CI workflow

| File | Reason |
|---|---|
| `.github/workflows/ci.yml` | RC1 Critical #1 — checkout → Poetry install → pytest+coverage → artifact upload. Reviewed, not yet committed. |

**Commit:** Yes.
**Command:**
```
git add .github/workflows/ci.yml
```

---

## 2. Commit for CLI

| File | Reason |
|---|---|
| `src/finfluencer/cli.py` | Console-script entry point (`finfluencer.cli:app`), thin re-export of the existing Typer app. Reviewed, verified identical target. |

**Commit:** Yes.
**Command:**
```
git add src/finfluencer/cli.py
```

---

## 3. Commit for coverage remediation (R5/R6)

New test files:

| File | Maps to |
|---|---|
| `tests/unit/test_utils/test_io.py` | R6 Module 1 (`utils/io.py`) |
| `tests/unit/test_market/test_ingest_manual_bist100.py` | R6 Module 2 |
| `tests/unit/test_collect/test_quota.py` | R6 Module 4 |
| `tests/unit/test_market/test_run_confirmatory_analysis.py` | R6 Module 7 |
| `tests/unit/test_providers/test_english.py` | R6 Module 9 |

Modified test files:

| File | Maps to |
|---|---|
| `tests/unit/test_core/test_budgets.py` | R6 Module 3 |
| `tests/unit/test_core/test_config.py` | R6 Module 8 |
| `tests/unit/test_core/test_registry.py` | R6 Module 6 |

Package markers needed (untracked directories — must ship alongside their first test file or the package won't exist in git):

| File | Reason |
|---|---|
| `tests/unit/test_collect/__init__.py` | `test_collect/` is entirely untracked; needed for `test_quota.py` above. |
| `tests/unit/test_market/__init__.py` | `test_market/` is entirely untracked; needed for the two market test files above. |

**Commit:** Yes.
**Command:**
```
git add tests/unit/test_utils/test_io.py \
        tests/unit/test_market/test_ingest_manual_bist100.py \
        tests/unit/test_market/test_run_confirmatory_analysis.py \
        tests/unit/test_market/__init__.py \
        tests/unit/test_collect/test_quota.py \
        tests/unit/test_collect/__init__.py \
        tests/unit/test_providers/test_english.py \
        tests/unit/test_core/test_budgets.py \
        tests/unit/test_core/test_config.py \
        tests/unit/test_core/test_registry.py
```

**Not included here, do not add with this command:**
- `tests/unit/test_core/test_reproducibility.py` — mixed content, see §8 below.
- `tests/unit/test_core/test_checkpoint.py` — Run Manifest System test (`TestAllMarkers`), see §5.
- `tests/unit/test_collect/test_run_pipeline_manifest.py` — Run Manifest System test, see §5.
- `tests/unit/test_market/test_confirmatory_analysis.py` (different name from `test_run_confirmatory_analysis.py`) — not part of any R6 module I built; unknown provenance, see §8.

---

## 4. Commit for v1.0 documentation

| File | Reason |
|---|---|
| `LICENSE` | §2.2 — MIT license matching `pyproject.toml`'s claim. |
| `.env.example` | §2.14 — documents `ANON_SALT`/`YT_API_KEY`/`EVDS_API_KEY`. |
| `README.md` (modified) | §2.15 — full rewrite from the 1-line stub. |
| `CHANGELOG.md` | §2.16 |
| `CONTRIBUTING.md` | §2.17 |
| `CITATION.cff` | §2.18 |
| `src/finfluencer/py.typed` | §2.5 — PEP 561 marker; grouped with docs items in the plan (Typing classifier accuracy). |

**Commit:** Yes — but note two of these files link to documents that are themselves not yet staged anywhere in this plan: README.md links to `Software_Product_Architecture_v1.0.md` and `ADR-0001_TopicEvolutionRecord_ScopeId_Deferral.md`; CHANGELOG.md references `Version_1.0_Release_Checklist.md`. All three linked files are currently untracked and land in §8 (Unknown) below because they predate this sprint and weren't part of my three audits. If they're committed elsewhere, that's fine; if not, these links will 404 for anyone browsing the repo on GitHub. Flagging, not blocking — your call on whether to fold them in.

**Command** (docs files are independent of each other; can be one commit or several — shown as one per the plan's grouping):
```
git add LICENSE .env.example README.md CHANGELOG.md CONTRIBUTING.md CITATION.cff src/finfluencer/py.typed
```

---

## 5. Commit for production fixes

This category covers both the Run Manifest System (pre-existing, reviewed, not yet committed) and this sprint's new v1.0 bug fixes. Several files mix logically distinct changes and **cannot be staged with a single plain `git add`** — noted explicitly below.

### 5a. Run Manifest System — clean, whole-file adds

| File | Reason |
|---|---|
| `src/finfluencer/core/reproducibility.py` | `RunStatus`, `generate_run_id()`, extended `build_provenance()`. Reviewed, clean, backward-compatible. |
| `tests/unit/test_collect/test_run_pipeline_manifest.py` | End-to-end manifest-wiring test. Reviewed this session, 4 tests collect cleanly. |

```
git add src/finfluencer/core/reproducibility.py tests/unit/test_collect/test_run_pipeline_manifest.py
```

### 5b. Run Manifest System — `checkpoint.py`, MIXED, needs split

`src/finfluencer/core/checkpoint.py`'s working-tree diff contains two distinct hunks:
- `all_markers()` method — Run Manifest System, pre-existing.
- "Concurrency" docstring section — this sprint's §2.13 fix.

A plain `git add src/finfluencer/core/checkpoint.py` stages **both** in one shot. If you want them as two separate commits (matching "one logical change per commit"), this file needs `git add -p` (interactive hunk selection) rather than a single command — I can't produce one `git add <file>` that cleanly separates them. Options: (a) commit both hunks together as one "Run Manifest System + concurrency docs" commit, or (b) use `git add -p` interactively. Flagging for your decision.

`tests/unit/test_core/test_checkpoint.py` (`TestAllMarkers` class) pairs with the `all_markers()` hunk only — clean single-purpose diff, no split needed.

```
git add tests/unit/test_core/test_checkpoint.py
# checkpoint.py itself: see note above before staging
```

### 5c. `pyproject.toml` — MIXED, needs split (already flagged in self-review)

Two unrelated changes are mixed in this file's diff:
- A pre-existing, unreviewed `yfinance`/market-extras block (not part of any of my three audits).
- This sprint's own edits: `youtube-transcript-api` dependency, version bump `0.1.0` → `1.0.0`, corrected dependency comment.

Per plan §5, the pre-existing change should be committed first, as-is, before layering my edit on top — this requires `git add -p` or a hand-built patch, not a single `git add pyproject.toml` (that would merge both into one commit, exactly what the plan says to avoid). Also note: `poetry.lock` was never regenerated for the new dependency (blocked — Python 3.11 floor vs. this sandbox's 3.10.12) — committing the dependency change now means `poetry.lock` will be stale until you run `poetry lock` locally.

```
# Requires git add -p (two separate commits) — not a single command.
```

### 5d. `collect/main.py` — MIXED, larger than the plan described

The working-tree diff for this file is **not** just the Run Manifest System. It bundles five distinct things:
1. Run Manifest System wiring (`_write_manifest_safe`, `run_id`, `RunStatus` — matches plan §2.1).
2. The full Entity-Centric Migration v2 pipeline-stage integration (`embeddings`/`sentiment`/`topics`/`topic_sentiment`/`topic_evolution` stages, `_PROVIDER_STAGES`, new imports) — a large, separate body of work from earlier in the engagement (tasks #5–#10), not something my three audits reviewed.
3. A torch-import-order DLL fix (dated 2026-07-14 in the code comment) — not mentioned in any audit or the plan.
4. Unexplained typography churn (Unicode arrows `→`/em-dashes `—` replaced with ASCII `->`/`-`) scattered through the docstrings — no clear origin, not reviewed by anyone.
5. This sprint's two fixes: quota-persist try/except (§2.7) and CLI exception-handling broadening (§2.8).

A single `git add src/finfluencer/collect/main.py` would attribute all five under one commit message, which is inaccurate and mixes reviewed with unreviewed material. This needs either your explicit sign-off to commit it as one "pipeline integration" commit, or `git add -p` to split it — same blocker I raised earlier in this session before you paused the sequence. Not staging until you decide.

```
# Do not add yet — needs your decision on how to split (see note above).
```

---

## 6. Research artifacts — DO NOT COMMIT

Everything below is manuscript/analysis output from the underlying research project, not release engineering. Outside this remediation's scope; provenance not something I can vouch for.

```
AnalysisScope_Impact_Analysis.md, CCR_OS_Architecture_v3.md, Coverage_Remediation_Plan.md,
Engineering_Status_Report_v2_Roadmap.md, Entity_Centric_Migration_Plan_v2.md,
Entity_Centric_Migration_v2_Closeout_Report.md, Graphical_Abstract*.png/svg (all variants),
Graphical_Abstract_Design_Spec.md, Hakem_Raporu_Brutal_Inceleme.md,
Hakem_Raporu_v2_Yeniden_Inceleme.md, Hakem_Raporu_v3_Makale_SON.md,
Migration_Roadmap_Reassessment.md, Production_Code_Audit.md, Production_Validation_Checklist.md,
RC1_Release_Readiness_Report.md, Referee_Report_Brutal_Review.md, Release_Readiness_Report.md,
Repository_Hygiene_and_Release_Remediation_Plan.md, Topic_Evolution_Production_Execution_Guide.md,
Topic_Evolution_Regeneration_Runbook.md, V1.0_Consolidated_Remediation_Plan.md,
V1.0_Documentation_Audit_and_Remediation_Plan.md, basaran_rebuild_orchestration_plan.md,
entity_centric_platform_architecture.md, service_oriented_multiplatform_architecture.md,
E5_dispersion_results.json, E5_topic_level_dispersion.csv, E5_video_level_dispersion.csv,
E6_sensitivity_results.json, E6_sensitivity_table.csv, E7_annotation_package_n500.csv,
E7_calibration_pilot_n30.csv, E7_sampling_frame_reference_INTERNAL_ONLY.csv,
R4_results.json, R5_results.json, R6_results.json, R7_results.json,
adjudication_log_complete.csv, adjudication_received.csv, adjudication_round2_received.csv,
build_R4_R7_tables.py, build_e5_e6_tables.py, build_e7_tables.py, build_e7_tables_complete.py,
build_e7_tables_final.py, build_manuscript_data.py, build_stats_figures.py, build_stats_tables.py,
diagnose_sentiment_memory.py, dunn_posthoc_matrix.csv,
e7_adjudication_sheet_ROUND2_n4.csv/.xlsx, e7_adjudication_sheet_n15.csv/.xlsx,
e7_validation_results.json, e7_validation_results_COMPLETE.json, e7_validation_results_FINAL.json,
export_master_table.py, final_health_report.py,
finfluencer_tr_2025_E5_E6_results.xlsx, finfluencer_tr_2025_E7_validation_results.xlsx,
finfluencer_tr_2025_E7_validation_results_COMPLETE.xlsx, finfluencer_tr_2025_E7_validation_results_FINAL.xlsx,
finfluencer_tr_2025_R4_R7_recommended_results.xlsx, finfluencer_tr_2025_inferential_stats.xlsx,
gold_sample_allocation_log.csv, gold_sample_prelim_with_tiers.csv,
gold_standard_sample_n500_CODER_FACING_blinded.csv/.xlsx,
gold_standard_sample_n500_FULL_with_model_prediction.csv/.xlsx,
health_report.json, inferential_results.json,
manuscript_fig1_data.csv, manuscript_fig2_data.csv, manuscript_fig3_data.csv, manuscript_table1.csv,
manuscript_top_neg5.csv, manuscript_top_pos5.csv, manuscript_topic_results_full.csv,
master_table.csv, master_table_with_category.csv, mixed_model_results.json,
pub_data.json, pub_data_pull.py, recover_basaran_only.py,
run_R4.py, run_R5.py, run_R6.py, run_R7.py, run_inferential_tests.py,
sentiment_validation_allocation_log.csv, sentiment_validation_sample_n500.csv,
sentiment_validation_sample_n500_v2_stratified.csv, smoke_test_market_module.py,
stats_figures/ (entire directory),
topic_category_map.csv, topic_category_mapping.csv, topic_level_test_results.csv,
video_level_volume_sentiment.csv, weekly_sentiment_by_analyst.csv,
Version_1.0_Release_Checklist.md
```

Note: `Version_1.0_Release_Checklist.md` is referenced by `CHANGELOG.md` (which is going into §4). Listed here rather than §8 because, unlike the architecture/ADR docs, it's explicitly a release-process tracking document (sign-off checklist), not source-of-truth reference material — but flagging the same link-target risk as noted in §4.

**Commit:** No.

---

## 7. Generated files — DO NOT COMMIT

```
.coverage, coverage.xml,
checkpoints/collect_channels.jsonl, checkpoints/collect_comments.jsonl,
checkpoints/collect_transcripts.jsonl, checkpoints/collect_videos.jsonl,
checkpoints/preprocess_comments__basaran.done, checkpoints/quota_state.json,
data/raw/channels.parquet, data/raw/comments.parquet,
data/raw/transcripts.parquet, data/raw/videos.parquet,
src/finfluencer/__pycache__/__init__.cpython-310.pyc,
src/finfluencer/collect/__pycache__/main.cpython-310.pyc,
src/finfluencer/collect/__pycache__/main.cpython-314.pyc,
src/finfluencer/collect/__pycache__/videos.cpython-310.pyc,
src/finfluencer/core/__pycache__/checkpoint.cpython-310.pyc,
src/finfluencer/core/__pycache__/config.cpython-310.pyc,
src/finfluencer/core/__pycache__/config.cpython-314.pyc,
src/finfluencer/core/__pycache__/contracts.cpython-310.pyc,
src/finfluencer/core/__pycache__/contracts.cpython-314.pyc,
src/finfluencer/core/__pycache__/reproducibility.cpython-310.pyc,
src/finfluencer/preprocess/__pycache__/pipeline.cpython-310.pyc,
src/finfluencer/providers/platform/__pycache__/youtube.cpython-310.pyc,
tests/unit/test_core/__pycache__/test_budgets.cpython-310-pytest-9.1.1.pyc,
tests/unit/test_core/__pycache__/test_checkpoint.cpython-310-pytest-9.1.1.pyc,
tests/unit/test_core/__pycache__/test_config.cpython-310-pytest-9.1.1.pyc,
tests/unit/test_core/__pycache__/test_registry.cpython-310-pytest-9.1.1.pyc,
tests/unit/test_core/__pycache__/test_reproducibility.cpython-310-pytest-9.1.1.pyc,
.~lock.finfluencer_tr_2025_E5_E6_results.xlsx#,
.~lock.finfluencer_tr_2025_inferential_stats.xlsx#
```

All `.pyc`/`__pycache__` entries here show as "modified" only because they were already tracked in git before `.gitignore` was introduced (RC1 Major finding, deferred to v1.1 — untracking 108 `.pyc` + 17 data files is a separate, larger decision, explicitly out of scope for this sprint). The two `.~lock...#` files are LibreOffice's own transient lock files for currently-open `.xlsx` files.

**Commit:** No.

---

## 8. Unknown / requires manual decision

| File(s) | Why it's ambiguous |
|---|---|
| `config/analysts.yaml`, `config/settings.yaml` | Explicitly marked "not touched" in plan §5. Modified in the working tree by something outside this remediation; provenance unknown to me. |
| `core/config.py`, `core/contracts.py`, `providers/market/__init__.py` | Same — explicitly out of scope per plan §5, but showing as modified. |
| `poetry.lock` | Tied to the `pyproject.toml` dependency change (§5c) but can't be regenerated in this sandbox (Python 3.10.12 < project's 3.11 floor). Needs `poetry lock` run locally before it can be committed alongside the dependency change. |
| `src/finfluencer/__main__.py`, `src/finfluencer/collect/comments.py` | Diffs against HEAD appear to be line-ending/BOM noise only (visually identical content) — not a change I made intentionally. Could be a tooling artifact; your call on whether to normalize and commit or leave alone. |
| `src/finfluencer/analysis/`, `src/finfluencer/embeddings/`, `src/finfluencer/market/`, `src/finfluencer/migration/`, `src/finfluencer/sentiment/`, `src/finfluencer/topics/`, `src/finfluencer/scope.py`, `src/finfluencer/providers/market/tcmb_evds_provider.py`, `src/finfluencer/providers/market/yfinance_provider.py` (all untracked, whole directories/files) | **Not research artifacts — this is production source code.** `collect/main.py`'s Entity-Centric Migration wiring (§5d) directly imports from `analysis.topic_sentiment`, `embeddings.pipeline`, `embeddings.sentence_transformer`, `sentiment.pipeline`, `sentiment.transformer_classifier`, and `topics.pipeline`. If `main.py` is ever committed without these, the import chain breaks for anyone checking out that commit. None of this was covered by my three audits (Documentation/RC1/Code Audit) — it predates this remediation sprint. |
| `tests/unit/test_analysis/`, `tests/unit/test_embeddings/`, `tests/unit/test_migration/`, `tests/unit/test_sentiment/`, `tests/unit/test_topics/`, `tests/unit/test_core/test_analysis_scope.py`, `tests/unit/test_scope.py`, `tests/unit/test_providers/test_market_providers.py` | Test suites paired with the source directories above — same "not part of my audits, but real source" situation. |
| `tests/unit/test_market/test_confirmatory_analysis.py` | Distinct from `test_run_confirmatory_analysis.py` (which IS an R6 module, in §3). Not something I built or reviewed; unclear if superseded, duplicate, or separate coverage. |
| `Software_Product_Architecture_v1.0.md`, `ADR-0001_TopicEvolutionRecord_ScopeId_Deferral.md` | Linked from `README.md`/`CONTRIBUTING.md` (going into §4) but predate this sprint and weren't part of my three audits. Needed for the doc links to resolve; not verified by me for accuracy. |
| `pyproject.toml`'s pre-existing `yfinance`/market-extras hunk | Covered in §5c — unrelated to my work, unreviewed, needs its own commit before my edit lands on top. |

**Commit:** Not without your decision on each row. The source-code rows (analysis/, embeddings/, market/, migration/, sentiment/, topics/, scope.py) are the highest-priority ones to resolve — they determine whether `collect/main.py` can be committed at all without breaking the import chain.

---

## Summary

| Category | File count (approx.) | Ready to stage now |
|---|---|---|
| 1. CI | 1 | Yes |
| 2. CLI | 1 | Yes |
| 3. Coverage remediation | 10 | Yes |
| 4. v1.0 documentation | 7 | Yes (2 dangling links noted) |
| 5. Production fixes | 2 clean + 3 needing split | Partial — 5a only |
| 6. Research artifacts | ~95 | No (by design) |
| 7. Generated files | ~28 | No (by design) |
| 8. Unknown | ~20 paths/dirs | No — needs your input |

No files have been staged. Waiting for your go-ahead on which commit to prepare first.
