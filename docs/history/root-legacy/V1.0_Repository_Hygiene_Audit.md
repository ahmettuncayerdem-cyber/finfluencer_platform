# Repository Hygiene Audit

No staging, committing, resetting, or deleting performed. Read-only classification of every currently untracked path (`git status` re-run fresh against `HEAD` = `96f924d`), plus a note on the tracked-but-modified files that are also cluttering the working tree.

---

## Category A — commit in the current release

Source, tests, docs, config that are part of v1.0 and either already verified or next in the dependency-ordered sequence.

| Path | Why A |
|---|---|
| `src/finfluencer/embeddings/`, `sentiment/`, `topics/`, `analysis/` | Entity-Centric Migration v2 pipeline stages; required by `collect/main.py`. Next four commits (4–7) per the dependency graph. |
| `tests/unit/test_embeddings/`, `test_sentiment/`, `test_topics/`, `test_analysis/`, `test_core/test_analysis_scope.py`, `test_scope.py` | Paired 1:1 with the source above. |
| `src/finfluencer/cli.py` | Console-script entry point, reviewed, no dependency issues. |
| `.github/` (`ci.yml`) | CI workflow, reviewed. |
| `src/finfluencer/py.typed`, `LICENSE`, `.env.example`, `CHANGELOG.md`, `CONTRIBUTING.md`, `CITATION.cff` | Reviewed v1.0 documentation/packaging items, no code dependency. |
| `ADR-0001_TopicEvolutionRecord_ScopeId_Deferral.md`, `Software_Product_Architecture_v1.0.md` | Linked from `README.md`/`CONTRIBUTING.md` (already staged conceptually as docs) — without these, those links 404. |
| `tests/unit/test_utils/test_io.py`, `tests/unit/test_providers/test_english.py` | R6 coverage-remediation tests with no unmet dependency (test their already-committed/HEAD-stable targets). |
| `tests/unit/test_collect/` (`__init__.py`, `test_quota.py`) | R6 coverage test, package marker. (`test_run_pipeline_manifest.py` in the same dir is Run Manifest System — same directory, different logical commit; see note below.) |

---

## Category B — commit later (valid, but future dependency-ordered commits)

| Path | Why B |
|---|---|
| `src/finfluencer/market/` (6 files) | Self-contained subsystem, not required by `main.py`. Needs `providers/market/` (below) landed first for `collect_market_data.py` specifically. |
| `src/finfluencer/providers/market/tcmb_evds_provider.py`, `yfinance_provider.py` | Must land together with the modified (tracked) `providers/market/__init__.py`, which now imports both — commit as a trio, not yet, since `__init__.py`'s import lines aren't isolated from the rest of that file's diff. |
| `pyproject.toml`'s pre-existing yfinance/market-extras hunk | Tied to the trio above; needs `git add -p` to isolate from this sprint's own `pyproject.toml` edits (unrelated, already flagged). |
| `tests/unit/test_market/` (`__init__.py`, `test_ingest_manual_bist100.py`, `test_run_confirmatory_analysis.py`, `test_confirmatory_analysis.py`) | Paired with `market/`. `test_confirmatory_analysis.py` specifically tests `confirmatory_analysis.py`/`figures.py`/`sentiment_index.py`, not something I built — include only once you've confirmed you want that coverage in v1.0. |
| `tests/unit/test_providers/test_market_providers.py` | Paired with the provider trio. |
| `src/finfluencer/migration/` (`backfill_entity_model.py`, `backfill_topic_scope.py`) | One-off scripts from when the migration was actually run against real data once. Nothing else depends on them. Your call whether they belong in v1.0 at all (historical record) or get deferred indefinitely — not blocking either way. |
| `tests/unit/test_migration/` | Paired with `migration/`. |
| `tests/unit/test_collect/test_run_pipeline_manifest.py` | Run Manifest System test — logically belongs with the `core/reproducibility.py`/`core/checkpoint.py` Run Manifest commit (already scoped in the earlier staging plan), not with the R6 coverage-test commit it happens to share a directory with. |
| `Version_1.0_Release_Checklist.md` | Referenced by `CHANGELOG.md`; lower-stakes dangling link than the ADR/architecture doc. Optional — commit alongside docs if you want the link to resolve, otherwise no functional impact. |
| `config/analysts.yaml` (modified, tracked) | Real roster data, 11-line diff, provenance not reviewed by me — glance at the diff before deciding whether it ships with this release. |

---

## Category C — ignore via `.gitignore`

Only two *currently untracked* items genuinely belong here — everything else that looks like ignore-fodder (`.pyc`, `__pycache__`, `.coverage`, `coverage.xml`, `checkpoints/`, `data/raw/*.parquet`) is **already tracked in git** (shows as `M`, not `??`), so adding a `.gitignore` pattern for those paths would be a no-op against the current pollution — it only prevents *new* files matching the pattern from becoming untracked-noise later. Untracking what's already committed is a separate, larger action (RC1 Major finding, deliberately deferred to v1.1 during the earlier remediation — 108 `.pyc` + 17 data/checkpoint files, explicitly out of scope for this release per your own earlier scope boundary).

| Path | Why C |
|---|---|
| `.~lock.finfluencer_tr_2025_E5_E6_results.xlsx#`, `.~lock.finfluencer_tr_2025_inferential_stats.xlsx#` | LibreOffice's own transient lock files for currently-open `.xlsx` files. Pure noise, regenerates automatically. |
| `err.txt` | Stray redirect-output file from an earlier diagnostic command in this session (`2>err.txt`). Not part of any deliverable. |

---

## Category D — stay outside the repository

Manuscript/research outputs and this engagement's own internal process documents. Not part of the shipped v1.0 product surface; provenance and accuracy not something I can vouch for as release engineering.

**Research data/analysis outputs** (CSV/JSON/XLSX results, figures, adjudication/annotation samples, one-off analysis scripts):
```
AnalysisScope_Impact_Analysis.md, CCR_OS_Architecture_v3.md, Coverage_Remediation_Plan.md,
Engineering_Status_Report_v2_Roadmap.md, Entity_Centric_Migration_Plan_v2.md,
Entity_Centric_Migration_v2_Closeout_Report.md, Graphical_Abstract*.png/svg (all variants),
Graphical_Abstract_Design_Spec.md, Hakem_Raporu_Brutal_Inceleme.md,
Hakem_Raporu_v2_Yeniden_Inceleme.md, Hakem_Raporu_v3_Makale_SON.md,
Migration_Roadmap_Reassessment.md, Referee_Report_Brutal_Review.md,
basaran_rebuild_orchestration_plan.md, entity_centric_platform_architecture.md,
service_oriented_multiplatform_architecture.md, Topic_Evolution_Production_Execution_Guide.md,
Topic_Evolution_Regeneration_Runbook.md,
E5_dispersion_results.json, E5_topic_level_dispersion.csv, E5_video_level_dispersion.csv,
E6_sensitivity_results.json, E6_sensitivity_table.csv, E7_annotation_package_n500.csv,
E7_calibration_pilot_n30.csv, E7_sampling_frame_reference_INTERNAL_ONLY.csv,
R4_results.json, R5_results.json, R6_results.json, R7_results.json,
adjudication_log_complete.csv, adjudication_received.csv, adjudication_round2_received.csv,
build_R4_R7_tables.py, build_e5_e6_tables.py, build_e7_tables.py, build_e7_tables_complete.py,
build_e7_tables_final.py, build_manuscript_data.py, build_stats_figures.py, build_stats_tables.py,
diagnose_sentiment_memory.py, dunn_posthoc_matrix.csv,
e7_adjudication_sheet_ROUND2_n4.csv/.xlsx, e7_adjudication_sheet_n15.csv/.xlsx,
e7_validation_results.json, e7_validation_results_COMPLETE.json, e7_validation_results_FINAL.json,
export_master_table.py, final_health_report.py,
finfluencer_tr_2025_E5_E6_results.xlsx, finfluencer_tr_2025_E7_validation_results.xlsx,
finfluencer_tr_2025_E7_validation_results_COMPLETE.xlsx, finfluencer_tr_2025_E7_validation_results_FINAL.xlsx,
finfluencer_tr_2025_R4_R7_recommended_results.xlsx, finfluencer_tr_2025_inferential_stats.xlsx,
gold_sample_allocation_log.csv, gold_sample_prelim_with_tiers.csv,
gold_standard_sample_n500_CODER_FACING_blinded.csv/.xlsx,
gold_standard_sample_n500_FULL_with_model_prediction.csv/.xlsx,
health_report.json, inferential_results.json,
manuscript_fig1_data.csv, manuscript_fig2_data.csv, manuscript_fig3_data.csv, manuscript_table1.csv,
manuscript_top_neg5.csv, manuscript_top_pos5.csv, manuscript_topic_results_full.csv,
master_table.csv, master_table_with_category.csv, mixed_model_results.json,
pub_data.json, pub_data_pull.py, recover_basaran_only.py,
run_R4.py, run_R5.py, run_R6.py, run_R7.py, run_inferential_tests.py,
sentiment_validation_allocation_log.csv, sentiment_validation_sample_n500.csv,
sentiment_validation_sample_n500_v2_stratified.csv, smoke_test_market_module.py,
stats_figures/ (entire directory),
topic_category_map.csv, topic_category_mapping.csv, topic_level_test_results.csv,
video_level_volume_sentiment.csv, weekly_sentiment_by_analyst.csv
```

**This engagement's own internal process documents** (audits, plans, checklists I generated — valuable as a record but not release engineering output):
```
Production_Code_Audit.md, Production_Validation_Checklist.md, RC1_Release_Readiness_Report.md,
Release_Readiness_Report.md, Repository_Hygiene_and_Release_Remediation_Plan.md,
V1.0_Category8_Dependency_Analysis.md, V1.0_Consolidated_Remediation_Plan.md,
V1.0_Documentation_Audit_and_Remediation_Plan.md, V1.0_Staging_Plan.md
```
(This document itself, once saved, belongs in this same list.)

**Commit:** No, for all of Category D.

---

## 1. Proposed `.gitignore` update

Only additive; nothing here retroactively affects already-tracked files.

```gitignore
# --- Editor / office lock files ---
.~lock.*#

# --- Ad-hoc diagnostic/command output ---
err.txt

# --- Python bytecode (prophylactic — prevents *new* untracked .pyc noise;
#     does NOT affect the 108 .pyc files already tracked in git from before
#     this pattern existed. Untracking those is a separate, deferred action.) ---
__pycache__/
*.py[cod]

# --- Coverage tooling output (same caveat: .coverage/coverage.xml are
#     already tracked; this only stops re-adding them by accident going
#     forward, e.g. via `git add .`) ---
.coverage
coverage.xml
htmlcov/
```

I did not add `checkpoints/`, `data/raw/*.parquet`, or `poetry.lock` patterns: the first two are already-tracked data files (an ignore pattern would be silently ineffective and could mislead future contributors into thinking they're excluded when they're not), and `poetry.lock` is supposed to be tracked — it's not clutter, it's just currently stale pending regeneration.

---

## 2. Files currently polluting the working tree

Beyond the untracked items above, these **already-tracked** files show as modified and add noise to every `git status`/`git diff` call, without being part of any commit prepared so far:

```
.coverage, coverage.xml,
checkpoints/collect_channels.jsonl, checkpoints/collect_comments.jsonl,
checkpoints/collect_transcripts.jsonl, checkpoints/collect_videos.jsonl,
checkpoints/preprocess_comments__basaran.done, checkpoints/quota_state.json,
data/raw/channels.parquet, data/raw/comments.parquet,
data/raw/transcripts.parquet, data/raw/videos.parquet,
poetry.lock,
9 tracked .pyc files under __pycache__/ directories
```

These are all pre-existing pollution (tracked before `.gitignore` conventions were introduced), not something this release workflow created. Deliberately not touching them — matches the scope boundary you drew earlier in this engagement.

---

## 3. Recommended cleanup sequence (no execution yet)

1. Commit the `.gitignore` update above as its own small commit — purely additive, zero risk, stops new noise immediately.
2. Delete (locally, your call, outside git) `err.txt` and the two `.~lock...#` files if the underlying `.xlsx` files aren't currently open — these are the only two Category C items and they're genuinely disposable. I'm not doing this myself per your "do not delete anything" instruction; flagging as safe whenever you want to.
3. Continue the dependency-ordered Category A commits (4 → 7: `embeddings/`, `sentiment/`, `topics/`, `analysis/`, then their tests, then `main.py`'s wiring).
4. Category B (market subsystem, migration scripts) whenever you decide they belong in this release — nothing forces them before v1.0 ships.
5. Category D is not a git action at all — it's manuscript/process material that simply never gets `git add`-ed. No cleanup needed; it only "pollutes" `git status` output, not the actual commit history.

---

## 4. Verification — no required source code at risk

Cross-checked this classification against the two prior analyses (`V1.0_Staging_Plan.md`, `V1.0_Category8_Dependency_Analysis.md`) and the current `git status`: every file that showed up as required-by-dependency in the earlier graph (`contracts.py`, `config.py`, `scope.py` — now committed; `embeddings/`, `sentiment/`, `topics/`, `analysis/` — Category A, next in sequence) is accounted for and correctly placed. Nothing in Category C or D is imported by anything in Category A or B — confirmed earlier via `grep` across every subsystem's `import finfluencer.*` lines. No source code is at risk of being silently dropped by this classification; Categories C and D only affect `git status` noise and `.gitignore`, not what eventually ships.

---

## 5. Next step

Working tree is now fully classified. Per your instructions, Commit 4 (`embeddings/`) preparation starts only after you confirm this audit — I have not yet run dependency/import verification on `embeddings/` itself. Say the word and I'll do that next.
