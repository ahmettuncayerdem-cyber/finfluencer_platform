"""Integration tests (BACKLOG.md T-013 onward).

Distinct from `tests/unit` — these exercise real subprocesses, real signals, and real
filesystem state across process boundaries, not just in-process object graphs.
"""
